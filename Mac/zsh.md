```shell
#确保Xcode已安装
#1.brew安装zsh
brew install zsh
#2.切换到zsh shell
chsh -s /usr/local/bin/zsh
#3.使用oh-my-zsh配置(zsh star数最多的配置)
curl -L https://github.com/robbyrussell/oh-my-zsh/raw/master/tools/install.sh | sh
```
